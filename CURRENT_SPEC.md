# LoL Matchup Viewer 現行仕様書

最終確認日: 2026-09-01  
対象バージョン: 0.1.3
根拠: 現在のソースコードおよび `data/` の実体

## 1. 製品概要

League of Legends の試合中に、自分とレーン対面のチャンピオンについて、通常攻撃射程、Q/W/E/R の射程、危険コンボの有効射程、警戒すべきスキルのクールダウンを即座に比較する Windows デスクトップアプリ。

Electron 製の単一ウィンドウアプリで、LoL クライアントが試合中に公開するローカル Live Client Data API を監視し、対面を自動表示する。自動判定できない場合は敵チャンピオンを手動選択できる。

## 2. 対象環境と配布形態

- 対象 OS: Windows（NSIS インストーラー）
- アプリ名: LoL Matchup Viewer
- アプリ ID: `com.lolmatchup.viewer`
- 現行バージョン: `0.1.3`
- インストール先: インストーラー上で利用者が変更可能
- 実行時の Node.js: 不要
- ネットワーク利用:
  - 通常利用時は LoL のローカル API `https://127.0.0.1:2999` のみ参照
  - データ同期時は League of Legends Wiki にアクセス

## 3. UI仕様

### 3.1 ウィンドウ

- 初期サイズ: 480 × 720 px
- 最小サイズ: 400 × 560 px
- タイトル: `LoL Matchup Viewer`
- 背景色: `#08111d`
- コンテンツ最大幅: 520 px
- 通常時の余白: 上 34、左右 38、下 28 px
- OS 標準のタイトルバーを使用する
- Windows では全ウィンドウを閉じるとアプリを終了する
- 開発モードのみ DevTools を別ウィンドウで自動表示する

### 3.2 デザイン共通

- ダークテーマ固定。OS のダーク配色として描画する
- フォント: Inter。未導入の場合は Segoe UI、sans-serif の順で代替
- 背景: 濃紺ベースに右上から青い放射グラデーション
- 数値比較では敵側を黄〜橙色で強調する
- セクション見出しは小さな英大文字、広い字間、灰青色
- チャンピオン名は英大文字表示
- 画面内スクロールはブラウザ標準挙動。専用スクロール制御はない
- キーボードショートカット、ツールチップ、設定画面、メニュー、通知はない
- チャンピオン画像、スキルアイコン、ロールアイコンは表示しない

### 3.3 画面状態

UI は `waiting`、`detected`、`manual`、`error` の4状態を持つ。

#### A. 試合待機画面 (`waiting`)

表示条件:

- 起動直後
- Live Client API に接続できない
- API が 200 以外を返す
- 1.4秒以内に応答しない
- レスポンスを解析できない
- アクティブプレイヤーを特定できない
- 試合終了などにより、それまで取得できていた API が利用不能になる

表示内容:

- 画面中央に直径 44 px の回転ローダー
- 見出し `Waiting for game...`
- 補足 `Leagueの試合開始を検出すると自動で表示します`
- 縦方向も画面中央寄せ

待機中に同じ失敗が続く場合は再描画しないが、バックグラウンドのポーリングは続ける。

#### B. 自動検出済み画面 (`detected`)

ヘッダー:

- ラベル `LIVE MATCHUP`
- タイトル `{自分のチャンピオン名} vs {敵チャンピオン名}`

本文は次の順で表示する。

1. `AA RANGE`
   - 3列: 項目名 / YOU / ENEMY
   - AA の基本攻撃射程を比較
   - 敵側数値は大きい黄文字
2. `SKILL RANGE`
   - Q、W、E、R を各1行で表示
   - 3列: スロット / YOU / ENEMY
   - 値が `null` の場合は `—`
   - 数値以外の文字列表現もそのまま表示する（例: `550 – 750`、`AA`、`700 / 900`）
3. `DANGER RANGE`
   - 敵の手動メタデータに登録された危険コンボを表示
   - 左にコンボ、右に有効射程
   - 最初に出現する `>` だけを ` → ` に置換して表示
   - 赤系グラデーション、左端に赤いアクセント線
   - 登録がなければ `No danger combo metadata`
   - `note` は現行 UI では表示しない
4. `KEY COOLDOWNS`
   - メタデータで指定された敵スキルのみ表示
   - 左にスロットとスキル名、右にランク別 CD を ` / ` 区切りで表示し末尾に `s`
   - CD データがない場合は `—s`
   - 登録がなければ `No key cooldown metadata`
5. `Change enemy` ボタン
   - 押下するとローカル UI 上だけ手動選択画面へ遷移する

#### C. 手動選択画面 (`manual` かつ敵未選択)

ヘッダー:

- ラベル `MANUAL SELECT`
- タイトル `{自分のチャンピオン名} vs ?`

本文:

- 説明メッセージ
- 敵チャンピオンのプルダウン
- 初期選択肢 `Select enemy...`
- 候補はチャンピオン英名の昇順
- 候補が Live Client API 上の敵チームから解決できた場合は敵チームだけに絞る
- 敵候補を1件もローカルデータへ解決できない場合、全チャンピオンを候補にする
- 選択すると即時に手動マッチアップ画面へ遷移する。確定ボタンはない
- 検索入力、ロール絞り込み、最近使った候補はない

#### D. 手動マッチアップ画面 (`manual` かつ敵選択済み)

- 内容は自動検出済み画面と同じ
- ヘッダーラベルのみ `MANUAL MATCHUP`
- 手動選択は永続保存しない
- Live Client の自動検出結果が変わると、自動結果で上書きされる

#### E. データエラー画面 (`error`)

表示条件:

- Live Client API から自分のチャンピオン名は取得できたが、対応するローカルチャンピオンデータがない

表示内容:

- 見出し `データが見つかりません`
- 詳細 `No local data for {取得名}.`
- 待機画面と同じ中央寄せレイアウト

敵だけがローカルデータにない場合はエラーにせず、手動選択画面へ移る。

### 3.4 更新と画面遷移

- Live Client Data API を2秒間隔で確認する
- 1回の HTTP タイムアウトは1.4秒
- 自分と敵の組み合わせを `{you}:{enemy}` という指紋で保持する
- 同じ組み合わせが続く限り、状態の再発行と UI 再描画を行わない
- 対面未確定時は敵部分を `manual` として指紋化する
- API エラーで待機状態へ戻る際に指紋を消去し、次回復帰時に同じ組み合わせでも再描画できるようにする
- レンダラー起動時に現在状態を1回取得し、その後はメインプロセスからの変更通知を購読する

### 3.5 レスポンシブ挙動とアクセシビリティ

- タイトル文字サイズは 25〜35 px の範囲で画面幅に追従する
- ウィンドウ最小幅と `body` 最小幅はいずれも 400 px
- それ以外の主要寸法は固定値で、専用モバイルレイアウトはない
- `select` と `button` は横幅100%
- HTML の特殊文字は画面描画前にエスケープする
- ARIA 属性、フォーカスリングの独自定義、スクリーンリーダー向け補足、色覚対応テーマは未実装

## 4. 対面自動検出仕様

### 4.1 取得元

- URL: `https://127.0.0.1:2999/liveclientdata/allgamedata`
- TLS 証明書の検証は無効化する（LoL ローカル API の自己署名証明書対応）
- HTTP 200 のみ成功扱い
- JSON 全体のうち `activePlayer` と `allPlayers` を利用する

### 4.2 自分の特定

`allPlayers` の各プレイヤーと `activePlayer` を次の順ではなく OR 条件で照合する。

- 両方に `riotId` があり一致
- 両方に `summonerName` があり一致

一致するプレイヤーがいない場合は検出失敗として待機画面になる。

### 4.3 敵の特定

1. 自分と `team` が異なる全プレイヤーを敵候補とする
2. `position` を大文字化し、`MIDDLE` を `MID` に正規化する
3. 自分の `position` と一致する最初の敵を採用する
4. 一致がなければ `MID` の最初の敵を採用する
5. それもなければ対面未確定として手動選択へ移る

位置情報が不正確・未提供のゲームモードでは正しいレーン対面を保証しない。ジャングル、ローム、レーンスワップ、複数人レーンを考慮した推定は行わない。

### 4.4 チャンピオン名の解決

- `rawChampionName` があれば通常形式の先頭 `game_character_displayname_`、またはCPU戦で使われる
  `Character_..._Name` の外側を除去して使用
- なければローカライズされ得る `championName` を使用
- ローカルデータとの照合時は Unicode NFKC 正規化、小文字化を行い、文字・数字以外をすべて除去
- `id`、英語 `name`、`aliases` のいずれかと一致すれば同一チャンピオンとする

## 5. 保持データ

### 5.1 データ全体

現時点のデータ実体:

- 正規化済みチャンピオン JSON: 172件
- 人手メタデータ JSON: 2件（Ahri、Syndra）
- Wiki 原文キャッシュ: チャンピオン一覧 Lua と各チャンピオン Q/W/E/R の Wiki テンプレート
- チャンピオンデータ取得日: 全件 `2026-08-30`
- パッチ表記: 全件 `current`。具体的なゲームパッチ番号は保持しない
- AA 射程の収録範囲: 125〜650
- `aliases` を持つチャンピオン: 21件

アプリ実行中は全チャンピオンデータと全メタデータをメインプロセスのメモリへ読み込む。DB は使わず、実行中の編集・追記・永続保存も行わない。

### 5.2 ディレクトリ

| パス | 内容 | 更新方法 | アプリ UI で使用 |
|---|---|---|---|
| `data/champions/*.json` | UI向けに正規化した客観データ | 同期スクリプト | 使用する |
| `data/matchup-meta/*.json` | 警戒スキル・危険コンボの人手メタデータ | 手動編集 | 使用する |
| `data/raw/lol-wiki/*` | League of Legends Wiki の取得原文キャッシュ | 同期スクリプト | 直接は使用しない |

パッケージ版では `data/` 全体を Electron の `resources/data` へ同梱する。開発版ではプロジェクト直下の `data/` を参照する。

### 5.3 ChampionData

1ファイルにつき1チャンピオンを保持する。

| フィールド | 型 | 必須 | 意味 | 現行UIで表示 |
|---|---|---:|---|---:|
| `id` | string | 必須 | ファイル名にも使うスラッグ形式の内部ID | 選択肢の値のみ |
| `name` | string | 必須 | 英語表示名 | 表示する |
| `aliases` | string[] | 任意 | API名や別表記から解決する別名 | 表示しない |
| `attackRange` | number | 必須 | 基本攻撃射程 | 表示する |
| `spells.Q/W/E/R` | SpellData | 必須 | 各スロットのスキル情報 | 一部表示する |
| `source` | string | 任意 | 元チャンピオン Wiki ページ | 表示しない |
| `patch` | string | 任意 | データの対象パッチ表記 | 表示しない |
| `retrievedAt` | string | 任意 | 取得日 `YYYY-MM-DD` | 表示しない |

`id` は英名を小文字化し、アポストロフィとピリオドを削除、英数字以外の連続をハイフン化して生成する。

### 5.4 SpellData

| フィールド | 型 | 必須 | 意味 | 現行UIで表示 |
|---|---|---:|---|---:|
| `name` | string | 必須 | スキル英名 | キークールダウン対象時のみ |
| `range` | number \| string \| null | 必須 | 射程。複数値、範囲、AA連動を文字列でも保持 | 常時表示 |
| `cooldown` | number[] | 任意 | スキルランクごとの基礎CD秒 | メタ指定時のみ |
| `width` | number \| null | 任意 | スキル幅 | 表示しない |
| `radius` | number \| null | 任意 | 効果半径 | 表示しない |
| `castTime` | number \| string \| null | 任意 | 詠唱時間 | 表示しない |
| `projectileSpeed` | number \| string \| null | 任意 | 弾速 | 表示しない |
| `collision` | string \| null | 任意 | 衝突対象の表現 | 表示しない |
| `targetType` | string \| null | 任意 | 対象指定方式 | 表示しない |

現データの充足数（全172チャンピオン）:

| Slot | range | cooldown | width | radius | castTime | projectileSpeed | collision | targetType |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| Q | 133 | 159 | 76 | 60 | 168 | 93 | 86 | 163 |
| W | 95 | 160 | 23 | 83 | 166 | 54 | 34 | 165 |
| E | 128 | 162 | 38 | 77 | 169 | 80 | 48 | 164 |
| R | 102 | 161 | 41 | 95 | 171 | 60 | 45 | 164 |

注意: `null` や欠落は必ずしも「値が存在しない」ことを意味せず、Wiki テンプレート上に値がない、または同期パーサーで抽出できなかった場合を含む。

### 5.5 MatchupMeta

客観データとは分離し、同期スクリプトによる上書きから保護する。

| フィールド | 型 | 必須 | 意味 | 現行UIで表示 |
|---|---|---:|---|---:|
| `championId` | string | 必須 | 対象チャンピオンID | 直接は表示しない |
| `keyCooldowns` | `Q/W/E/R` の配列 | 必須 | 警戒対象として表示するスキル | 表示選定に使用 |
| `dangerCombos` | 配列 | 必須 | 危険コンボ一覧 | 表示する |
| `dangerCombos[].combo` | string | 必須 | `E>Q` などのコンボ表記 | 表示する |
| `dangerCombos[].effectiveRange` | number | 必須 | コンボの実効射程 | 表示する |
| `dangerCombos[].note` | string | 任意 | 人手による補足 | 現行UIでは非表示 |

現行内容:

| Champion | Key cooldowns | Danger combo | Effective range | Note |
|---|---|---|---:|---|
| Ahri | E | E → Q | 1000 | なし |
| Syndra | E | Q → E | 1300 | Sphere stun threat（UI非表示） |

この2体以外は AA/スキル射程比較は可能だが、危険コンボとキークールダウン欄は空状態メッセージになる。

### 5.6 実行時状態 MatchupState

| フィールド | 型 | 意味 |
|---|---|---|
| `status` | `waiting \| detected \| manual \| error` | 現在の表示状態 |
| `you` | ChampionData | 自分。待機・一部エラー時は未設定 |
| `enemy` | ChampionData | 敵。手動選択前は未設定 |
| `enemyMeta` | MatchupMeta | 敵に対応する人手メタ。未登録なら未設定 |
| `candidates` | ChampionData[] | 手動選択候補 |
| `message` | string | 手動選択理由またはエラー詳細 |

状態はメインプロセスのメモリ上だけに存在し、アプリ終了で消える。レンダラーとの受け渡しは Electron IPC を利用する。

## 6. データ同期仕様

`npm run sync:champions` で League of Legends Wiki から全チャンピオンを同期する。

処理概要:

1. `Module:ChampionData/data` の raw Lua を取得またはキャッシュから読む
2. 10進小数IDの別形態（例: Mega Gnar）は独立チャンピオンとして除外
3. AA射程と Q/W/E/R のテンプレート名を抽出
4. 各スキルの raw Wiki テンプレートを取得
5. UI向け JSON へ正規化して `data/champions/` に書き出す
6. Wiki 由来でなくなった旧チャンピオン JSON を削除する
7. `data/matchup-meta/` は変更しない

通常同期は raw キャッシュを優先し、`npm run sync:champions -- --refresh` で再取得する。HTTP 取得は最大4回再試行し、待機時間を指数的に増やす。アクセス間隔として成功ごとに150ms待機する。解析チャンピオン数が150未満の場合は部分更新を拒否する。

射程値は Wiki の表記に応じて単一数値、範囲文字列、複数値文字列、`AA`、`null` のいずれかになる。CD は抽出した数値配列として保持し、`x to y` 表記は5ランク分へ線形補間する。

## 7. IPC・セキュリティ境界

レンダラーへ公開する API は次の3つだけ。

- `getState()`: 現在状態を取得
- `selectEnemy(championId)`: 敵を手動選択
- `onStateChanged(callback)`: 状態変更通知を購読し、解除関数を返す

Electron の `contextIsolation` は有効、`nodeIntegration` は無効。レンダラーはファイルシステムや Node.js API へ直接アクセスしない。

## 8. 現行の制約・未実装

- マッチアップ固有の立ち回り、ルーン、ビルド、勝率、レベル別有利不利は扱わない
- 自分または敵のスキルレベル、アイテム、ヘイストを考慮した実CD計算はしない
- 射程図、画面オーバーレイ、常に手前表示、クリック透過、ホットキーはない
- 対面手動選択や設定を再起動後に保持しない
- メタデータ編集 UI はない
- データ更新通知、自動更新、パッチ整合性チェックはない
- 具体的な LoL パッチ番号を保持せず `current` のため、データの鮮度は取得日で判断する必要がある
- メタデータは Ahri と Syndra のみ
- `note`、`width`、`radius`、`castTime`、`projectileSpeed`、`collision`、`targetType` は保持しているが画面には出さない
- Wiki 記法の複雑な値が完全には正規化されず、文字列断片が混入する場合がある。現データにも例として Ahri の弾速に `|}}` 等が残っている
- 1つのスキルが複数形態・複数射程を持つ場合も、Q/W/E/R 各1レコードの簡略モデルである
- `Change enemy` は中央状態を即時変更せずレンダラー内で選択画面を描くだけなので、その直後に同一内容の状態通知が来ると自動表示へ戻る可能性がある
- 同一の自分・敵の組み合わせでは指紋が変わらないため、実行中にデータファイルを更新しても自動再読込・再描画しない

## 9. 主要技術構成

- Electron 37
- TypeScript 5.7
- Vite 7
- UI フレームワークなし（DOM文字列描画 + CSS）
- 永続DBなし（JSONファイル）
- electron-builder / NSIS による Windows 配布
